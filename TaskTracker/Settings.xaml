<Window x:Class="TaskTracker.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskTracker"
        mc:Ignorable="d"
        Title="Settings" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ListView x:Name="Categories" d:ItemsSource="{d:SampleData ItemCount=5}">
            
        </ListView>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="Add" Height="30" MinWidth="50"/>
            <Button Content="OK" Margin="10,0,0,0" MinWidth="50"/>
        </StackPanel>
    </Grid>
</Window>
